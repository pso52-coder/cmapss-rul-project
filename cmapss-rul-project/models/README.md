# models/

이 폴더는 학습된 모델, scaler, threshold 설정 파일을 저장하기 위한 위치입니다.

현재 GitHub에는 `.pkl`, `.joblib` 모델 파일을 업로드하지 않습니다.  
모델과 scaler는 재현 스크립트를 실행하면 로컬에서 다시 생성하는 것을 원칙으로 합니다.

- `threshold_config.json`: FD001 기준 후보 운영 threshold 정보
- `.gitkeep`: 빈 폴더 유지를 위한 파일
